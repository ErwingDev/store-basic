import { UseInterceptors } from '@nestjs/common';
// import { DynamicUploadInterceptor } from '../interceptor/file.interceptor';

export const UploadImage = (folder: string, field = 'image') => null;
    // UseInterceptors(DynamicUploadInterceptor(folder, field));
